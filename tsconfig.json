{
  "files": [],
  // 项目引用列表: 将整个工程的部分内容分离为单独模块，让它能独立配置TS 编译项，进行独立的构建
  // 当本项目被编译时，依赖的项目引用也会被自动编译
  "references": [
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.web.json" }
  ]
}
